name: Convert README.md to PDF and Commit

on:
  push:
    paths:
      - README.md
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y pandoc texlive-xetex git

      - name: Convert README.md to resume-kiron-ang.pdf with specific margins
        run: pandoc README.md -o resume-kiron-ang.pdf --pdf-engine=xelatex -V geometry:margin=0.25in

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Commit and push PDF if changed
        run: |
          git add resume-kiron-ang.pdf
          if ! git diff --cached --quiet; then
            git commit -m "Auto-update resume PDF [skip ci]"
            git push origin HEAD:${{ github.ref_name }}
          else
            echo "No changes to commit"
          fi
